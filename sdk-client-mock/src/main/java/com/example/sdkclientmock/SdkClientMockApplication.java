package com.example.sdkclientmock;

/**
 * Simple entry point for the SDK client mock. The application now avoids
 * external frameworks so it can build and run in environments without network
 * access.
 */
public class SdkClientMockApplication {
    public static void main(String[] args) {
        System.out.println("sdk-client-mock is running");
    }
}
